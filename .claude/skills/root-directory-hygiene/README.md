# root-directory-hygiene

**Tier**: beta (proven in CMT_Codebase, not yet reused elsewhere)

## Purpose

>

## Installation

```
cp -r root-directory-hygiene/ <project>/.claude/skills/
```

Then commit to the project's repo.

## Provenance

Ported from `CMT_Codebase/.claude/skills/repo-hygiene/` on 2026-04-22.

Renamed during port: `repo-hygiene` -> `root-directory-hygiene` (category-first convention).

## Slash-ref rewrites applied during port

- `/repo-hygiene\b` -> `/root-directory-hygiene` (7 occurrences)
